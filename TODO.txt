CHARTS

	LINEAR
		Two different Axis delegates by default							OK
		Refactoring code in LinearAxisDelegate::paintBack()
		  It should be simplier to paint the ticks
	
	KIVIAT			
		Bar style does not work

	PIE
		Selection does not work											~Ok
			Selecting series in the legend view does not work
		Painting method is ugly.

	ALL
		Create Properties when needed